# Application
APP_NAME="Enterprise RAG System"
APP_VERSION="1.0.0"
DEBUG=false
SECRET_KEY="your-secret-key-change-in-production-please"

# Server
HOST=0.0.0.0
PORT=8000

# MySQL 8.0 (Docker 内部连接)
DATABASE_URL=mysql+pymysql://root:123456@mysql:3306/docbase_knowledge
DATABASE_POOL_SIZE=10
DATABASE_MAX_OVERFLOW=20

# LLM Provider: deepseek
LLM_PROVIDER=deepseek
DEEPSEEK_API_KEY=replace_with_your_deepseek_api_key
DEEPSEEK_BASE_URL=https://api.deepseek.com
DEEPSEEK_MODEL=deepseek-v4-flash

# Embedding Provider: ollama | huggingface
EMBEDDING_PROVIDER=huggingface

# Ollama Embedding (如果使用 Ollama，需要额外部署)
OLLAMA_BASE_URL=http://host.docker.internal:11434
OLLAMA_EMBEDDING_MODEL=qwen3-embedding:4b

# HuggingFace Embedding
HF_EMBEDDING_MODEL=BAAI/bge-m3
HF_EMBEDDING_DEVICE=cpu
HF_NORMALIZE_EMBEDDINGS=true

# Vector Store (Chroma)
CHROMA_PERSIST_DIR=./chroma_db
CHROMA_COLLECTION_NAME=rag_documents

# File Upload
UPLOAD_DIR=./uploads
MAX_FILE_SIZE=104857600
ALLOWED_EXTENSIONS=pdf,doc,docx,txt,xls,xlsx,jpg,jpeg,png,gif,bmp

# RAG Config
CHUNK_SIZE=1000
CHUNK_OVERLAP=200
TOP_K=5
RERANK_TOP_K=5
RETRIEVAL_SCORE_THRESHOLD=0.1
MAX_CONTEXT_LENGTH=6000

# Java Backend API (Docker 内部连接)
JAVA_API_KEY=replace_with_internal_agent_api_key
JAVA_BASE_URL=http://docbase-backend:8080
